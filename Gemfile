source 'https://rubygems.org'

# Reference
# http://bundler.io/v1.10/gemfile.html

# Need this for $ bundle platform command to show ruby version
# $ bundle platform doesn't see the similar entry in bs_ruby_regex.gemspec
ruby '2.1.3'

# Specify your gem's dependencies in bs_ruby_regex.gemspec
gemspec

